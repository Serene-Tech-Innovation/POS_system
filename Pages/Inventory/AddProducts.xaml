<Window x:Class="POS.Pages.Inventory.AddProducts"
       
 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Add Item to Inventory">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Add Item to Inventory"
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Product Name -->
        <StackPanel Grid.Row="1" Margin="0,5">
            <TextBlock Text="Enter name of product:"/>
            <TextBox x:Name="productNameTextBox" Width="300" Height="30"/>
        </StackPanel>

        <!-- Product Price -->
        <StackPanel Grid.Row="2" Margin="0,5">
            <TextBlock Text="Enter price of product:"/>
            <TextBox x:Name="productPriceTextBox" Width="300" Height="30"/>
        </StackPanel>

        <!-- Product Image -->
        <StackPanel Grid.Row="3" Margin="0,5">
            <TextBlock Text="Enter image:"/>
            <Button Content="Upload Image" Width="150" Height="30" Click="UploadImage_Click"/>
            <Image x:Name="productImage" Width="100" Height="100" Margin="10"/>
        </StackPanel>

        <!-- Submit Button -->
        <StackPanel Grid.Row="4" Margin="0,20">
            <Button Content="Submit" Width="100" Height="30" Click="SubmitButton_Click"/>
        </StackPanel>
    </Grid>
    </Window>
