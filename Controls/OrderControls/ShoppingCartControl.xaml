<UserControl x:Class="POS.ShoppingCartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"             
             xmlns:av="http://schemas.microsoft.com/expression/blend/2008"  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel>
        <TextBlock Text="Cart Items" FontSize="18" TextAlignment="Center" Margin="0,0,0,10"/>

        <ListView x:Name="cartListView" ItemsSource="{Binding CartItems}" Height="600" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="90" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Price/Unit" Width="65" DisplayMemberBinding="{Binding Price}"/>
                    <GridViewColumn Header="Total" Width="40" DisplayMemberBinding="{Binding TotalPrice}"/>
                    <GridViewColumn Header="Qty" Width="65">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox x:Name="quantityDropDown"
                                  Width="50"
                                  Tag="{Binding}"
                                  SelectedValue="{Binding Quantity, Mode=TwoWay}">
                                    <ComboBox.ItemsSource>
                                        <x:Array Type="{x:Type sys:Int32}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <sys:Int32>1</sys:Int32>
                                            <sys:Int32>2</sys:Int32>
                                            <sys:Int32>3</sys:Int32>
                                            <sys:Int32>4</sys:Int32>
                                            <sys:Int32>5</sys:Int32>
                                            <sys:Int32>6</sys:Int32>
                                            <sys:Int32>7</sys:Int32>
                                            <sys:Int32>8</sys:Int32>
                                            <sys:Int32>9</sys:Int32>
                                            <sys:Int32>10</sys:Int32>
                                        </x:Array>
                                    </ComboBox.ItemsSource>
                                </ComboBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Action" Width="55">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="❌" Background="Red" x:Name="btnRemove" Tag="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Label x:Name="lblTotal" Content="Total: Rs.0" FontSize="16" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal">
            <Button x:Name="btnCheckout" Content="Checkout" Background="LawnGreen" 
                    Width="100" Margin="0,0,10,0"/>
            <Button x:Name="btnClear" Content="Clear" Background="Red" Width="100"/>
        </StackPanel>
    </StackPanel>
</UserControl>