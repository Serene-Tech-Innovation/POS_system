<UserControl x:Class="POS.InventoryEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:POS.Models.Core">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Product Editing Grid -->
        <DataGrid x:Name="ProductGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Grid.Row="0"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, Mode=TwoWay}" Width="80"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock, Mode=TwoWay}" Width="60"/>
                <DataGridTemplateColumn Header="Category" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding CategoryList}"
                                      DisplayMemberPath="Name"
                                      SelectedItem="{Binding Category, Mode=TwoWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Subcategory" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding Category.Subcategories}"
                                      DisplayMemberPath="Name"
                                      SelectedItem="{Binding Subcategory, Mode=TwoWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Action buttons -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Apply" Margin="2" Padding="5,0" Click="ApplySingle_Click"/>
                                <Button Content="Delete" Margin="2" Padding="5,0" Click="Delete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Optional Apply All -->
        <Button Grid.Row="1" Content="Apply All" Height="30" Width="100" HorizontalAlignment="Right" Margin="0,10,0,0"
                Click="ApplyAll_Click"/>
    </Grid>
</UserControl>
